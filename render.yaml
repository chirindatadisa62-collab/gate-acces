services:
  - type: web
    name: gate-access-system
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn gate:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0